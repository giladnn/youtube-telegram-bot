docs/plans/
docs/brainstorms/
docs/ideation/

# Environment variables (secrets)
.env
.env.local
.env*.local

# Standalone nested repo — has its own git history
victoria-neiman-website/

# Telegram session (LIVE ACCOUNT LOGIN — never commit)
*.session
*.session-journal

# Runtime data (regenerated by the bot)
state.json
summaries.json
summaries.html
summaries/

# Node
node_modules/
package.json
package-lock.json
